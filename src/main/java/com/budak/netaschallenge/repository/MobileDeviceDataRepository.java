package com.budak.netaschallenge.repository;

import com.budak.netaschallenge.domain.MobileDeviceData;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * @author Samet BUDAK
 * @since
 */
public interface MobileDeviceDataRepository extends JpaRepository<MobileDeviceData, Long> {
}
